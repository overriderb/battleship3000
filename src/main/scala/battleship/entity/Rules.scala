package battleship.entity

/**
  * TODO: Add comment
  */
object Rules {

  val mapSize = 10
  // Map of key - decker size to value - count on the map
  val ships = Map[Int, Int]((1, 4), (2, 3), (3, 2), (4, 1))

}
